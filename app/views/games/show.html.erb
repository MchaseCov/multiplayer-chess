<%=@game.white_player.username%>
<%=@game.color_player ? @game.color_player.username : "No second player yet" %>
<%= turbo_stream_from @game %>
<%= turbo_frame_tag id="chess_#{dom_id(@game)}", class:"w-full" do%>
  <%= cache @game do%>
    <%= render @game%>
  <% end %>
<% end %>
